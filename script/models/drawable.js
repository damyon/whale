'use strict';

class Drawable {
  constructor() {
  }

  initBuffers(gl) {
  }

  animateTextureCoordinates(gl, textureCoordBuffer, deltaTime) {
  }

  draw(gl, programInfo, buffers, texture, deltaTime) {
  }
}
